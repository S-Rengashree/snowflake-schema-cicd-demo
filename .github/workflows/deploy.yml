name: Deploy Snowflake Objects

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Snowflake CLI
      - name: Install Snowflake CLI
        run: |
          python -m pip install --upgrade pip
          pip install snowflake-cli-labs

      # 3️⃣ Create private key file (PKCS#8 – newline safe)
      - name: Configure Snowflake private key
        run: |
          mkdir -p ~/.ssh
          cat <<'EOF' > ~/.ssh/rsa_key_unencrypted.p8
          ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          EOF
          chmod 600 ~/.ssh/rsa_key_unencrypted.p8

      # 4️⃣ Create Snowflake connection config
      - name: Configure Snowflake connection
        run: |
          mkdir -p ~/.snowflake
          cat <<EOF > ~/.snowflake/config.toml
          [connections.snowflake]
          account = "${{ secrets.SNOWFLAKE_ACCOUNT }}"
          user = "${{ secrets.SNOWFLAKE_USER }}"
          role = "${{ secrets.SNOWFLAKE_ROLE }}"
          warehouse = "${{ secrets.SNOWFLAKE_WAREHOUSE }}"
          database = "${{ secrets.SNOWFLAKE_DATABASE }}"
          schema = "${{ secrets.SNOWFLAKE_SCHEMA }}"
          authenticator = "SNOWFLAKE_JWT"
          private_key_path = "~/.ssh/rsa_key_unencrypted.p8"
          EOF
          chmod 600 ~/.snowflake/config.toml

      # 5️⃣ Test connection (fail fast)
      - name: Test Snowflake connection
        run: |
          snow sql --connection snowflake --query "SELECT CURRENT_USER(), CURRENT_ROLE();"

      # 6️⃣ Deploy SQL files
      - name: Deploy Snowflake objects
        run: |
          for file in tables/*.sql; do
            echo "Deploying $file"
            snow sql --connection snowflake --filename "$file"
          done
